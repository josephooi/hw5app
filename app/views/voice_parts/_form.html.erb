<%= form_for(@voice_part, html: {class: "form-horizontal"}) do |f| %>
  <% if @voice_part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@voice_part.errors.count, "error") %> prohibited this voice_part from being saved:</h2>

      <ul>
      <% @voice_part.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="offset2">
  <div class="control-group">
    <%= f.label :name,
                class: 'control-label' %>
    <div class="controls">
      <%= f.text_field :name %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :max,
                class: 'control-label' %>
    <div class="controls">
      <%= f.text_field :max%>
    </div>
  </div>

</div>

<div class="form-actions" style="text-align: center">
  <%= f.submit class: "btn btn-large btn-primary" %>
</div>
<% end %>
